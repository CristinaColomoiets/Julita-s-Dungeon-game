class Player {

    constructor(gameSize) {

        this.gameSize = gameSize

        this.playerSize = {
            w: 65,
            h: 65, 
        }

        this.playerPos = {
            left: leftValue,
            top: topValue
        }
    }
}